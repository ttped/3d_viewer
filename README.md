# TechViewer 3D

A lightweight, React-based 3D model viewer designed to be dropped into existing workflows. It features a sidebar for system selection, a search bar for isolating specific parts within a 3D assembly, and zero external styling dependencies (standard CSS modules).

## 🚀 Quick Start

### 1. Prerequisites
Ensure you have Node.js installed (v18+ recommended).

### 2. Installation
Install the required 3D libraries and icon pack:

```bash
npm install three @types/three @react-three/fiber @react-three/drei lucide-react